<%= button_to title || t(".title"),
      stripe_checkout_path(plan: plan),
      method: :post,
      data: {
        turbo: false,
        controller: "turbo-native--in-app-purchases",
        action: "turbo-native--in-app-purchases#purchase",
        "turbo-native--in-app-purchases-plan-value": Businesses::Plan.with_identifier(plan).revenue_cat_product_identifier
      },
      class: local_assigns[:classes] || "bg-red-600 text-white hover:bg-red-700 mt-8 block w-full py-3 px-6 border border-transparent text-center font-medium cursor-pointer" %>
